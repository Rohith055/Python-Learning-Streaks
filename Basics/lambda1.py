# Here we are going to create a lambda func
# In python The functions are objects
# Here we can able to convert the funcs as a Object 

f  = lambda a,b : a+b
# Here the lambda function should contains any number of variable's should contains one line expression

# The values of a&b is passed in this 
result = f(5,6)
print(f)